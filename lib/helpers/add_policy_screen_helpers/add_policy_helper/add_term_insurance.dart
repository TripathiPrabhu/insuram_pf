import 'package:flutter/material.dart';

class AddTermInsurance extends StatelessWidget {
  const AddTermInsurance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
